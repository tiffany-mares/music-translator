# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

LyraLearn (repo: music-translator) has completed the entire `architecture.md` §10 build plan except Phase 2.6 (chunked-timing validation, blocked on a g4dn quota case) — the paragraph below narrates every phase in order. Phases 1.1 (local Demucs separation), 1.2 (faster-whisper transcription), 1.3 (MarianMT translation), and 1.4 (Basic Pitch melody extraction) are done — all four ML stages validated independently on the same test song; see `notes/phase1.md` for the recorded baselines and decisions (model size, forced alignment, translation granularity, pitch quality). Setup: `py -3.11 -m venv lyralearn-env && source lyralearn-env/Scripts/activate && pip install -r requirements.txt` (PowerShell: `lyralearn-env\Scripts\Activate.ps1`). Separation: `python scripts/check_gpu.py` then `python -m demucs.separate -n htdemucs --two-stems vocals -o output/stems test_data/input_song.mp3`, validated by `python scripts/validate_stems.py`. Transcription: `python scripts/run_transcription.py --model-size large-v3` (dumps JSON + line/word SRT to `output/`). Translation: `python scripts/run_translation.py` (fills translatedText, dumps JSON + RO/EN review file to `output/`). Pitch: `python scripts/run_pitch_extraction.py` (dumps melody.mid + pitch JSON + synth preview to `output/`). Tests: `python -m pytest tests/ -v`. Phase 1.5 (full pipeline) is done: `python pipeline.py [song] [songId] [output-dir]` (defaults: `test_data/input_song.mp3`, `test-song-001`, `output/`) runs Demucs → faster-whisper + Basic Pitch concurrently → MarianMT in one command and writes `song_lyrics.json` (section 6.2 shape, checked by `python scripts/validate_song_lyrics.py`), `melody.mid`, `pitch.json`, and `timings.json`. Measured CPU timings for the 215s test song: Demucs 591s, Whisper+pitch (concurrent) 1001s, translation 30s, total 1622s (~27 min). Phase 2.1 (containerized single SageMaker run) is done: `docker build --platform linux/amd64 -f container/Dockerfile -t lyralearn-ml:2.1 .` builds the consolidated container (no weight baking yet — that's 2.2's delta), `scripts/aws/run_processing_job.sh` triggers the manual ml.g4dn.xlarge Processing Job, and `python scripts/compare_s3_output.py output output/sagemaker-2.1` is the done-when gate against the Phase 1 baseline (PASS on job lyralearn-2-1-20260727-004408; GPU in-container total 72.4s vs 1622s local CPU); see `notes/phase2.md` for the recorded cold-run numbers 2.2 must beat and the two load-bearing container env fixes (MKL_THREADING_LAYER=GNU, LD_LIBRARY_PATH exposing torch's bundled cuDNN to ctranslate2). Phase 2.2 (baked model weights, image `lyralearn-ml:2.2`) is done with an inverted finding: baking did NOT drop start-to-first-inference (142.3s→156.4s — AWS-internal model downloads are ~free, the bigger image pull is not) but is kept for HF-Hub dependency isolation; in-container ML total is 60.3s. See `notes/phase2.md` §2.2. Phase 2.3 (linear Step Functions pipeline) is done: `scripts/aws/deploy_state_machine.sh` deploys `lyralearn-pipeline` (MarkProcessing → RunMLPipeline → RunTranslation → MarkComplete/Failed, DynamoDB `LyraLearnTable` job items, `lyralearn-translate` container Lambda writing §6.2 lyrics to S3), `scripts/aws/start_pipeline_execution.sh <songId> <jobId>` triggers it; done-when evidence (green run + forced bad-audio failure hitting MarkFailed) is in `notes/phase2.md` §2.3, along with a live-observed §4 sketch bug (Retry on the SageMaker task self-collides on job name — removed; fix the chunked variant the same way in 2.4). **The Terraform commitment is FULFILLED (Phase 3.0, 2026-07-27):** all 24 AWS resources live under `terraform/` (§7 module layout, S3 backend `lyralearn-tfstate-503233513399`), `terraform plan` is clean, and infra changes now go through `cd terraform && terraform plan/apply` — ALWAYS prefixed by `eval "$(aws configure export-credentials --profile new-profile-name --format env)"` (the provider can't read root login sessions; exported tokens last ~15 min, keep applies short) and ALWAYS plan-checked for `0 to destroy` first. Lambda image pushes stay with `scripts/aws/deploy_*_lambda.sh`; the two state-machine deploy scripts are superseded by Terraform. Phase 3.1 (Cognito + DynamoDB foundation) is done: pool `us-east-1_m1EBuRGMm` / client `3a0g52nr1supgrak3u1lbambp6`, GSI1/2/3 + WebSocketConnections live, verified by `scripts/verify_3_1.sh` (PASS — see `notes/phase3.md`). Phase 3.2 (core Python Lambda routes) is done: HTTP API `https://v7iyrsczl5.execute-api.us-east-1.amazonaws.com` with a Cognito JWT authorizer (ID tokens, not access tokens) fronting the `lyralearn-api` Lambda — `POST /songs`, `GET /jobs/{id}` (composite id `{songId}.{jobKey}`), `GET /songs/{id}/lyrics` (S3-backed until 3.5), `GET /songs/{id}/audio-urls` (driven by the METADATA item's `audioKeys` map); verified by `scripts/verify_3_2.sh` (PASS — see `notes/phase3.md` §3.2). **Phase 3.5 MUST include the MongoDB migration** (Atlas + Secrets Manager + RunTranslation dual-write + lyrics-route swap + backfill — user decision 2026-07-27). Phase 3.3 (Rust validation Lambda) is done: `lyralearn-validate` (Rust/provided.al2023, built with `scripts/build_validate_lambda.sh` via the cargo-lambda Docker image — no host Rust) serves `POST /songs/{id}/process`, enforcing 50KB–25MB size bounds and magic-byte format checks (mp3/wav/flac/ogg/m4a) with verdicts written to the METADATA item (`VALIDATED`/`REJECTED`); it deliberately does not start Step Functions yet (3.5's wiring). Verified by `scripts/verify_3_3.sh`, including a measured zero-new-SFN-executions assertion (see `notes/phase3.md` §3.3). Phase 3.4 (chromaprint fingerprinting + GSI3 dedup) is done: the fingerprint module lives in `lyralearn-validate` itself (no new Lambda) — `rusty-chromaprint`/`symphonia`-based fingerprinting feeds a `GSI3PK = FP1#<simhash32 hex>` candidate lookup plus acoustic verification (`match_fingerprints`, score ≤ 10, coverage ≥ 0.8), auto-linking a matched re-upload by writing `status=LINKED` + `linkedSongId` (no GSI3PK on the linked item, so the index stays canonical) and short-circuiting Step Functions entirely; a format-validated file that fails to decode/fingerprint (e.g. HE-AAC m4a, Ogg Opus) still lands `VALIDATED` with no dedup rather than being rejected. Verified by `scripts/verify_3_4.sh` (PASS, including the zero-new-Step-Functions-executions assertion) — see `notes/phase3.md` §3.4 for the full stack/decisions/CloudWatch record. Phase 3.5 (wire pass-path to Step Functions + MongoDB migration) is DONE (2026-07-31): `POST /songs/{id}/process` now starts `lyralearn-pipeline-chunked` on a cache miss (validate Lambda mints a 12-hex UUIDv4 jobKey = SFN execution name, pre-writes a QUEUED job item, returns composite jobId `{songId}.{jobKey}`; StartExecution failure → 500 with FAILED tombstone; decode-degrade also starts the pipeline, LINKED never does) and all five MongoDB migration obligations hold: Atlas M0 (`lyralearn.lyrics`, unique index on songId), secret `lyralearn/mongodb` shell-only in Terraform (value set out-of-band, never in TF state), translate Lambda best-effort dual-write, lyrics route Mongo-primary/S3-fallback discriminated by `X-Lyrics-Source`, and the S3→Atlas backfill (2 docs). New WriteAudioKeys ASL state publishes `audioKeys.vocals/noVocals` (explicit stem keys from StitchResults) before translation. Images `lyralearn-translate:3.5`/`lyralearn-stitch:3.5` (buildx needs `--provenance=false --sbom=false` — Lambda rejects OCI attestation manifests); pymongo ships via Docker-built layer (`scripts/build_api_layer.sh`) to the api Lambda. Verified by `scripts/verify_3_5.sh` (both paths end-to-end: one full ~35-min quota-limited chunked run + instant cache hit; see `notes/phase3.md` §3.5). The GSI3 double-start race is weighed and accepted (duplicate cost, zero corruption). Next: Phase 2.6 when the g4dn quota-6 case closes; Phase 4 (frontend) is unblocked. Phase 4.1 (frontend scaffold + auth) is DONE (2026-07-31): React 19 + TS + Vite app in `frontend/` (dev loop: `cd frontend && npm run dev` / `npm test`; 19 Vitest tests), Cognito SRP auth via aws-amplify v6 confined to `src/auth/AuthContext.tsx` (no router — conditional render is the route guard; `Amplify.configure` side-effect module MUST stay main.tsx's first import; `frontend/.env` public IDs committed by design), deployed at **https://d38bvqcndpelgt.cloudfront.net** (S3 `lyralearn-frontend-503233513399` + CloudFront `ENXKD14WMP6C9` with OAC, SPA 403→index.html fallback, CachingDisabled on index/CachingOptimized on /assets/*; deploy via `scripts/deploy_frontend.sh` — immutable assets first, no-cache index last; NOTE `MSYS_NO_PATHCONV=1` needed there but NOT in verify scripts, it breaks curl /dev/null). HTTP API now has CORS for the CloudFront domain + localhost:5173 (api module `frontend_origin` var, single-apply via `wait_for_deployment=false`). Real-user gate passed: signup → email code → confirm → authenticated shell, live on CloudFront (`scripts/verify_4_1.sh` + notes/phase4.md §4.1). **4.2 must send the ID token (`session.tokens.idToken`), never the access token.** Phase 4.2 (upload + job status) is DONE (2026-07-31): `UploadPanel` in the shell drives POST /songs → presigned PUT → POST /process with a discriminated-union outcome (`started|linked|rejected|startFailed`), React Query v5 polls GET /jobs with `min(2000*2^(n-1), 15000)` backoff and `refetchIntervalInBackground: true` (hidden tabs otherwise pause polling — observed live); all `fetch` calls live in `frontend/src/api/client.ts` (the test mock boundary), tokens fetched per-call via `useAuth().getIdToken()`. **Two live-run discoveries recorded in notes/phase4.md §4.2:** the audio bucket needed `aws_s3_bucket_cors_configuration` (browsers preflight presigned PUTs; curl gates never did — PUT-only rule for the CloudFront domain + localhost:5173, extend to GET when 4.3+ needs Web Audio API fetches), and jsdom invalidates `required` file inputs even with files attached (tests use `fireEvent.submit`). 40 frontend tests. Verified end-to-end live: cache-miss run `fb5b41e8c62d` (30s file, ~11 min at quota 1) watched QUEUED→PROCESSING→COMPLETE in the UI; instant LINKED on the deployed CloudFront app; `scripts/verify_4_2.sh` PASS. Next: 4.3 player shell (immediate playback via audio-urls). Phase 4.3 (player shell — immediate playback) is DONE (2026-07-31): `Player` (frontend/src/player/) renders a plain `<audio controls>` (no `crossorigin` → no-CORS media fetch → audio bucket CORS stays PUT-only until Web Audio in 4.4+) in UploadPanel's polling+linked branches; `useAudioUrls(songId, pipelineDone)` puts pipeline-done in the queryKey for exactly-one stems refetch at COMPLETE (keepPreviousData bridges); **sticky src** — every audio-urls fetch re-signs URLs, so the Player adopts a URL once per stem selection and never rebinds on refetch (else playback would restart at COMPLETE); default stem raw, picker Original/Vocals/Instrumental with currentTime-preserving switch. 51 frontend tests. Live-verified: audio at currentTime 3.9s→25.3s, paused:false, while job showed "Processing — ChunkAudio…" (playback beat the pipeline by ~10 min); stems hydrated without reload; deployed CloudFront LINKED path plays with all stems; `scripts/verify_4_3.sh` PASS. **Gotchas recorded in notes/phase4.md §4.3:** Chrome defers media loading in hidden tabs (readyState 0 forever until visible), and cache-miss test audio = ffmpeg-cut distinct 30s windows of input_song.mp3 (no fingerprint de-index needed). Next: 4.4 lyrics hydration. Phase 4.4 (lyrics hydration) is DONE (2026-07-31): `frontend/src/lyrics/` — pure `wordSync.ts` (flatten + binary search; word active from its start until the NEXT word's start, final word clears at its own end), `useLyrics` (enabled-gated by pipelineDone — lyrics 404 all through QUEUED/PROCESSING — `staleTime: Infinity`, one fetch), `useWordSync` (rAF over the **`<audio>` element clock — deliberate §5.1 deviation**, AudioContext without media routing is not a playback clock and routing no-CORS media through MediaElementAudioSourceNode is silent; Web Audio + bucket GET CORS = 6.4), `LyricsPanel` (active word as prop; spans carry data-start/end), wired into Player via `lyricsSongId` (linked songs fetch the ORIGINAL's doc — backend get_lyrics doesn't follow linkedSongId, future hardening). **Backend fix shipped with it:** missing lyrics returned 500 not 404 (S3 AccessDenied vs NoSuchKey without ListBucket) — caught ClientError, regression-tested, api Lambda redeployed. 82 frontend + 82 backend tests. Live-verified on CloudFront: hydration without reload at COMPLETE (navigation count 1), accuracy assertions matched mid-playback and end-state, linked path shows original's lyrics instantly; `scripts/verify_4_4.sh` PASS. Note: rAF pauses in hidden tabs (highlight freezes, snaps correct when visible). Next: 4.5 loading/error states (forced pipeline failure must surface a real error state). Phase 4.5 (loading/error states) is DONE (2026-07-31) — **PHASE 4 COMPLETE**: WordSyncedLyrics takes `pipelineState: 'running'|'failed'|'done'` (running → "Lyrics loading…", failed → null since JobStatusLine's alert owns the failure); UploadPanel's polling branch reads the deduped job query and surfaces "Try again" → `retryProcess` (fresh jobKey, polling restarts); Player shows alert+Retry on audio-urls error and alert+"Reload track" on media element error — reloadTrack awaits refetch and DIRECT-SETS the fresh URL (the setSrc(undefined)+adoption-effect route would re-adopt the stale cached URL; `res.isSuccess` guard load-bearing). **Forced-failure recipe**: `b'ID3'+os.urandom(61*1024)` passes validation, degrades past fingerprinting, fails ChunkAudio's sf.read in seconds — no SageMaker; used by `scripts/verify_4_5.sh` (PASS: FAILED with error + retry mints fresh jobId) and the live browser gate (alert + both affordances + retry cycle sampled: processing+lyricsloading → FAILED+tryagain). 92 frontend tests. Gotcha: any component newly using React Query hooks breaks tests still rendering bare AuthProvider (AuthPage.test migrated to renderWithProviders). Next: Phase 5 (learning service) or Phase 2.6 (quota-gated). Phase 5.1 (learning data + skeleton) is DONE (2026-07-31): `lyralearn-learning` — the first Java Lambda (java21, plain Java not Spring, maven-shade fat jar, handler `com.lyralearn.learning.Handler::handleRequest`, 512MB/10s) — serves 501 stubs on `POST /vocab/review` + `GET /vocab/due` behind the shared JWT authorizer. Build ONLY via `scripts/build_learning_lambda.sh` (docker maven:3.9-eclipse-temurin-21; host JDK is 26/OpenJ9 — unusable for Lambda; jar must exist before terraform plan). Pom pins `project.build.outputTimestamp` so the jar is byte-reproducible and the terraform hash only churns on real changes. IAM deliberately pre-grants the full Phase-5 charter (table Get/Put/UpdateItem + GSI2 Query) so 5.3 needs no IAM apply. Vocab schema live per §6.1 (`term`/`definition`/`easeFactor`/`intervalDays`/`repetitions`/`nextReviewAt` ISO-8601; GSI2SK=nextReviewAt — ISO strings make lexicographic = chronological, do NOT change the format); GSI2 due-today range query (`<= now`, exclusion verified) proven by `scripts/verify_5_1.sh` (PASS). Next: 5.2 SM-2 in isolation (§5.5 reference impl; JUnit harness seeded). Phase 5.2 (SM-2 in isolation) is DONE (2026-07-31): `SpacedRepetitionService.schedule()` + `UserVocabProgress` in lambda/learning — character-faithful to §5.5 with two documented deviations (Clock injection for testability; quality 0-5 validation, since q>5 would inflate EF via the negative (5-q) terms). Pinned spec semantics: EF NOT updated on failure, interval × UPDATED floored EF, floor 1.3, Math.round half-up. 17 in-container tests verify hand-computed sequences (all-5: 1/6/17/49/147 = canonical SM-2; all-4: 1/6/15/38/95 — the 37.5 half-up boundary held; all-3 decay; lapse-preserves-EF recovery). **NOT deployed** — next terraform plan shows 1 pending jar-hash change, expected; 5.3 wires POST /vocab/review + GET /vocab/due to DynamoDB and deploys (IAM pre-granted). Phase 5.3 (wire the endpoints) is DONE (2026-07-31): both `/vocab` routes live against DynamoDB — `POST /vocab/review` GetItems, runs `schedule()`, and upserts via one UpdateItem (SM-2 fields + lastReviewedAt + GSI2 keys; **create-on-first-review** when the body carries `term`/`definition`, 404 without them — there is deliberately no create-vocab endpoint, 5.5 creates-on-encounter); `GET /vocab/due` queries GSI2 (`<= now`, paginated, most-overdue first). Deploy was jar-hash-only (5.1's IAM pre-grant paid off: `0 to add, 1 to change, 0 to destroy`). Architecture: `VocabRepository` seam — thin `DynamoDbVocabRepository` (SDK v2 2.25.70 + url-connection-client, netty/apache excluded; jar 7.6MB) proven live, all logic unit-tested via `InMemoryVocabRepository` + fixed Clock; Gson without reflection. **Correctness invariants recorded in notes/phase5.md §5.3:** all persisted instants `truncatedTo(SECONDS)` (variable-precision `Instant.toString()` breaks GSI2's lexicographic ordering within a second) and GSI2SK reuses the same `:next` expression value as nextReviewAt so they can't drift. 31 in-container tests; `scripts/verify_5_3.sh` first-try PASS (review moves item out of `/vocab/due` immediately — the done-when). Phase 5.4 (quiz generation) is DONE (2026-07-31): new `GET /vocab/quiz` builds cloze questions from the user's due items with real lyric context from Atlas — question shape `{vocabId,term,definition,hasContext,songId,lineNumber,prompt,translation}` (context fields explicit JSON null when no context; blank `____`, ALL occurrences blanked; cap 20). Vocab items gained optional `songId` (review-body extension, GSI2 ALL projection propagates it; `/vocab/due` now emits it). **Load-bearing split recorded in notes/phase5.md §5.4:** Mongo does only a case-insensitive substring prefilter (regex-escaped, limit 5); whole-word matching lives in Java (`Cloze` — letter/digit lookarounds not `\b`, UNICODE_CASE, NO diacritic folding) because server-side PCRE `\b` isn't Unicode-aware for ă/ș/ț. `MongoLyricsRepository` is lazy (review/due cold starts unchanged) + Document-API-only (shade-safe); driver mongodb-driver-sync 5.1.4 + ServicesResourceTransformer; secret via `MONGODB_SECRET_ARN` env + `ReadMongoSecretOnly` IAM; learning Lambda timeout 10→15s (cold quiz stacks JVM + secret + Atlas TLS). 51 in-container tests; `scripts/verify_5_4.sh` first-try PASS (quiz prompt byte-equal to the real `test-song-001` line with the term blanked, vs an independent pymongo read — the done-when). Phase 5.5 (frontend integration) is DONE (2026-07-31) — **PHASE 5 COMPLETE**: the full loop works live on CloudFront — play a song (linked path), click a lyric word to save it (`frontend/src/vocab/` — encounter posts quality-0 create-on-first-review with `vocabId=term.toLowerCase()`, definition=line translation, songId=the lyrics doc's song so quiz context resolves), Review tab with due-count badge (Shell Listen/Review nav; views stay MOUNTED and toggle `hidden` so audio survives switching), due-today list + cloze quiz session (imperative one-shot snapshot in `useQuizSession`, NOT useQuery — answers invalidate `['vocab','due']` via `useReviewVocab`, the codebase's first useMutation), grades Again/Hard/Good/Easy → 0/3/4/5, "Next review: {date}" from the mutation response after each grade. Words render as native `<button>` only when a click handler is passed — span fallback kept every pre-5.5 test passing unchanged. 132 frontend tests / 21 files. Gates: `scripts/verify_5_5.sh` PASS (browser-shaped encounter → backdate → real-context quiz byte-equal → rescheduled out) + live browser walkthrough as the real user (cloze "Și te rog, ____ mea" from the linked original's Atlas doc; "Next review: Aug 1, 2026"; badge/list refreshed without reload) — see notes/phase5.md §5.5. Next: Phase 2.6 (quota-gated) or §10's next phase. Phase 6.1 (WebSocket connection lifecycle) is DONE (2026-08-01) — **the project's first Go code**: `lambda/ws/` (go 1.24 — current AWS SDK v2 floor; Docker-only via `scripts/build_ws_lambda.sh` which runs vet+tests then cross-compiles both zips reproducibly — in-container `tools/buildzip` sets the 0755 bootstrap mode Windows zips can't and pins the entry timestamp, verified identical sha256 across builds). WebSocket API `lyralearn-ws-api` live at `wss://wf10x3gbnl.execute-api.us-east-1.amazonaws.com/prod` (`terraform output ws_endpoint`; stage `prod` — WS APIs reject the `$default` stage name; `$default` ROUTE deferred YAGNI): `$connect` → `lyralearn-ws-connect` validates the same Cognito ID token as a `token` query param IN-HANDLER (WS APIs can't use the HTTP JWT authorizer; golang-jwt/v5 + hand-rolled JWKS cached per container — full check matrix unit-pinned incl. token_use=="id") then PutItems {connectionId, userId=sub} into WebSocketConnections (Put failure → 500 handshake rejection); `$disconnect` → `lyralearn-ws-disconnect` DeleteItems (Dynamo error → log+200; leaked rows are 6.2's GoneException class; no TTL per spec). Two per-function IAM roles (PutItem-only / DeleteItem-only). 19 in-container Go tests; `scripts/verify_6_1.sh` first-try PASS (row appears via GSI1 on connect with userId==sub, deleted on disconnect — the done-when; bad/missing token → 401 handshake refusal, no row; python `websockets` = test-only venv dep). Next: 6.2 push on job completion (Streams on LyraLearnTable → third Go function, PostToConnection + GoneException cleanup). Phase 6.2 (push on job completion) is DONE (2026-08-01): DynamoDB Streams (NEW_IMAGE) on LyraLearnTable → event source mapping with FilterCriteria (MODIFY + SK prefix JOB# — vocab/metadata writes never invoke it) → `lyralearn-ws-push` (third Go Lambda). **Three documented §5.6 deviations (notes/phase6.md §6.2):** owner resolved via SONG#{songId}/METADATA.uploadedBy GetItem (job items carry NO userId — the reference's extractUserIdFromKey can't work), fan-out to ALL of the user's GSI1 connections (multi-tab), and Handle always returns nil (at-most-once; a returned error re-drives the Streams batch and duplicates pushes — polling is the fallback). Push message == the GET /jobs polling contract byte-for-byte (errorInfo capped 500 runes) so 6.3 reuses the Job type; GoneException → ErrGone → stale-row DeleteItem. **Two recorded operational findings:** API GW idles WS connections at 10 min — clients need app-level keepalive frames (~240s; error frames from the route-less API don't close the socket; browsers can't protocol-ping from JS → 6.3 needs the same or reconnect + consider a no-op $default route); and an ESM created moments before its first records can MISS them (LATEST checkpoint set at first poll ~1-2 min after creation, observed + diagnosed via raw stream read). 39 in-container Go tests; `scripts/verify_6_2.sh` full PASS on a real pipeline run (PROCESSING push instant, **COMPLETE push +0.7s vs SFN stopDate** — the done-when; zero polling, structural). Next: 6.3 frontend WebSocket integration (polling stays the tested fallback). Phase 6.3 (frontend WebSocket integration) is DONE (2026-08-01): the WS push channel is now the PRIMARY status path — `JobSocketProvider` in Shell owns one `JobSocket` (`frontend/src/ws/`; fresh-token connect, 240s app-level keepalive, 1s→30s reconnect ladder, onclose-only state machine, StrictMode-safe) and writes every jobId-bearing frame into the shared `['job', jobId]` React Query cache, so UploadPanel/Player/JobStatusLine updated with zero component changes; `useJobPolling` gates its interval on socket health (healthy → 60s safety net since 6.2 pushes are at-most-once; unhealthy → the 4.2 backoff — context default false keeps every pre-6.3 test unchanged). `window.__cadenzaKillSocket` = the deliberate-kill gate affordance (kill≡stop, no reconnect). **Testing findings recorded in notes/phase6.md §6.3:** jsdom SHIPS a real WebSocket (tests would dial prod!) — `src/test/setup.ts` deletes the global so the suite is WS-less by default; post-kill polling resumes at the 15s CAP not 2s (setQueryData increments dataUpdateCount); Chrome FREEZES hidden/covered tabs (memory saver) — timed browser gates need the window visible. 155 frontend tests / 24 files. Live gate measured via the in-page Performance API: 60s poll cadence while WS-primary, kill → row deleted, no reconnect, next poll exactly 15s later returned COMPLETE → "Complete — lyrics are ready." via polling alone on a dead socket (the done-when). Next: 6.4 TensorFlow.js sing-along mode. Phase 6.4 (TensorFlow.js sing-along mode) is DONE (2026-08-01): a "Sing along" toggle on every mounted Player opens `frontend/src/singalong/` — pure CREPE decode math (`crepeDecode.ts`, reference-faithful linspace cents mapping `1997.379... + 7180*bin/359`, ±4-bin weighted decode, zero-mean/unit-std frames with silent→zeros), a Vite module worker (`crepeWorker.ts`) that is the ONLY importer of `@tensorflow/tfjs@4.22.0` (dynamic import; requires `worker: { format: 'es' }` in vite.config.ts — iife workers can't code-split; main bundle 377.6 KB, tfjs isolated in a 1.88 MB worker-only lazy chunk, grep-verified), and `useSingAlong` (mount = lazy boundary; 16 kHz AudioContext + ScriptProcessorNode(1024) mic frames with browser DSP off, transfer-list posts, worker-side last-wins backpressure + tf.tidy). Model load order IS the cache proof: `indexeddb://crepe` hit → else HTTP `/models/crepe/model.json` (marl/crepe MIT build vendored in frontend/public, 2.0 MB, LICENSE.md) → best-effort save; the permanent "Model ready in X.Xs — downloaded | loaded from cache (IndexedDB)" status line is the honest gate evidence (public/ deploys immutable, so HTTP-cache speed could masquerade — the tfjs source label can't). **v1 = mic-pitch detection only** (note + cents + Hz, confidence < 0.5 → "Listening…"); melody-target comparison DEFERRED (pitch.json has no serving endpoint — recorded prerequisite in notes/phase6.md §6.4). Live gate: first open 6.3s downloaded → same-session reopen **0.5s from IndexedDB** (12.6×, the done-when) → post-reload 0.8s from IndexedDB; real humming tracked (F#4 converging +12→+1 cents). Accepted debts: ScriptProcessorNode, Firefox 16 kHz context, StrictMode dev double-worker. 185 frontend tests / 27 files. Phase 6.5 (C++ DSP core) is EXPLICITLY SKIPPED (2026-08-01) via its done-when's "confirmed unnecessary" branch — **PHASE 6 COMPLETE**: no feature consumes tempo/beat data (pitch.json isn't even served — 6.4's deferred prerequisite), so a benchmark would measure data with zero readers; revisit trigger recorded in notes/phase6.md §6.5 (a rhythm-consuming feature + the pitch endpoint landing together). Every unconditional §10 build item is now done; project-wide remaining: 2.6 (quota-gated). Phase 2.4 (chunking) is done: `scripts/aws/deploy_chunked_state_machine.sh` deploys `lyralearn-pipeline-chunked` (ChunkAudio Lambda slices 40s/2.5s-overlap WAV chunks + manifest.json, Map fans out per-chunk `lyralearn-ml:2.4` jobs writing `chunkStartOffset` into their outputs), triggered via `SM_NAME=lyralearn-pipeline-chunked scripts/aws/start_pipeline_execution.sh`, gated by `python scripts/verify_chunk_outputs.py` (PASS on 6/6 chunks — see `notes/phase2.md` §2.4). The Map runs `MAX_CONCURRENCY=1` until the g4dn quota-6 request can be filed (one open request per quota; the 0→1 case is still open) — sequential chunking is ~7x slower than whole-song, parallelism is the entire point (2.6). Phase 2.5 (StitchResults) is done: `lyralearn-pipeline-chunked` is now the full pipeline (chunk → Map → stitch → translate; ASL at `infra/aws/pipeline-chunked.asl.json`, stitch Lambda deployed by `scripts/aws/deploy_stitch_lambda.sh`), verified end-to-end with translated lyrics from the stitched transcript. **Key finding (notes/phase2.md §2.5): §5.3's chunking tradeoff is real and quantified** — chunked output fails the whole-song comparison gate on vocals RMS (+25.7%, uniform demucs bleed on 40s windows, not a stitch bug) and text similarity (0.56 vs whole-song 0.71; seam duplicates at 2 of 5 boundaries), while the stitcher itself is unit-exact on clean seams. Boundary evidence via `python scripts/boundary_report.py output output/stitched-2.5 215.4`. Phase 2.6 (timing validation, needs quota-6) is next and should weigh the wall-clock win against this measured quality cost.

## What this is

A music-based language learning platform: upload a song, split it into stems, transcribe and translate the lyrics, extract melody/pitch data, and play it back in a word-synced interactive player with spaced-repetition vocab review.

## Architecture (v4 — fully serverless, chunked pipeline)

Everything runs on Lambda + API Gateway + DynamoDB + MongoDB Atlas. There is no EKS, RDS, VPC, or NAT Gateway in this design — that's a deliberate cost/simplicity choice (see `architecture.md` section 8), not an oversight. Don't reintroduce them without updating the architecture doc first.

### Pipeline (the core async flow)

```
Browser (React+TS) --upload--> S3 (pre-signed PUT)
  --> POST /songs/{id}/process --> Rust Lambda (validation + audio fingerprint)
        --> fingerprint matches an existing song? --> link songId directly, no pipeline run (near-instant)
        --> new song? --> Step Functions (STANDARD):
              ChunkAudio (Lambda) --> Map state, ~40s overlapping chunks in parallel:
                SageMaker Processing Jobs (GPU, ml.g4dn.xlarge), per chunk:
                  Demucs (two-stems) -> faster-whisper (large-v3) -> Basic Pitch [+ C++ DSP core via pybind11]
              --> StitchResults (Lambda): crossfade audio at overlaps, offset+merge/dedupe transcript & pitch data
              --> RunTranslation (Lambda) --> MarkComplete
  --> audio playback starts immediately once upload validation passes (doesn't wait on the pipeline)
  --> lyrics/translation/pitch data hydrate into the player progressively as they arrive
  --> job status pushed via API Gateway WebSocket API + Go Lambda ($connect/$disconnect/push,
      triggered by DynamoDB Streams) or polled via GET /jobs/{id} (React Query, backoff 2s->15s cap)
  --> playback events POST to Java Lambda learning service (SM-2 scheduling, writes to DynamoDB)
```

Latency: a fingerprint cache-hit is a DynamoDB lookup — near-instant, no pipeline run. A genuinely new song's full pipeline (transcription/translation/pitch, not playback which starts immediately) now runs in **~70-110s**, down from an original 8-13 min worst case, via chunked parallel SageMaker jobs. Chunking doesn't reduce billed compute (SageMaker bills per instance-second regardless) — it only collapses wall-clock time by running chunks concurrently. See `architecture.md` sections 2 and 4 for the full reasoning, and section 5.3's stated tradeoff: chunk boundaries falling mid-word/mid-note can degrade quality slightly versus a single unchunked job — validate this in Phase 2 rather than assuming the crossfade/offset logic fully hides it.

Everything sits behind API Gateway directly (HTTP API for REST routes, WebSocket API for `/ws`) — no ALB, no VPC Link, no ingress path to maintain.

### Services and their responsibilities

- **Python Lambda** — `/songs`, `/jobs/{id}`, `/songs/{id}/lyrics`, `/songs/{id}/audio-urls` — thin read/proxy layer over DynamoDB, MongoDB, and S3 pre-signing.
- **Rust Lambda** — upload validation *and* audio fingerprinting (chromaprint-based, not a raw file hash — catches re-encoded duplicates), added to the same hot-path Lambda rather than a separate function. On a fingerprint match it links the new `songId` to the existing song's data and returns without ever invoking Step Functions.
- **Step Functions (ASL)** — only runs for genuinely new songs (post-dedup). `ChunkAudio` fans the song into overlapping ~40s chunks via a `Map` state (`MaxConcurrency: 6`), each chunk processed as its own SageMaker job; `StitchResults` crossfades audio at the overlaps and offsets/merges/dedupes each chunk's transcript lines and pitch notes by `chunk_start_offset` before translation and completion.
- **Go Lambda functions** — three small functions (`$connect`, `$disconnect`, DynamoDB Streams-triggered push handler) backing the WebSocket API. Connection identity lives in the `WebSocketConnections` DynamoDB table, not in memory — Lambda is stateless/short-lived, so there's no persistent hub process. Deliberate tradeoff: Go's concurrency strengths aren't really in play here anymore.
- **Java Lambda** — learning service, implements SM-2 spaced repetition (same algorithm as Anki) in `SpacedRepetitionService.schedule()`. Persists via `DynamoDbClient.updateItem` on `USER#{userId}/VOCAB#{vocabItemId}` items; `GET /vocab/due` queries `GSI2` (`userId` + `nextReviewAt`). Plain Java Lambda, not Spring Boot — leaner cold start at this traffic volume.
- **C++ DSP core** — beat/tempo detection, exposed to the Basic Pitch Python container via pybind11 (`dsp_core` module). Only build this if Basic Pitch's stock output proves insufficient (Phase 6.5) — not speculative.
- **React/TS frontend** — progressive hydration player: playback starts as soon as the pre-signed audio URL exists, independent of pipeline completion; lyrics/translation/pitch UI elements activate individually as their data lands (via WebSocket push or polling), with a "lyrics loading..." placeholder in the interim. Web Audio API `AudioContext.currentTime` sampled on `requestAnimationFrame`, binary-searched against sorted word-timing data. TensorFlow.js CREPE model is lazy-loaded only when sing-along mode opens (not on initial load), cached in IndexedDB, and runs in a Web Worker against mic input so it doesn't block the main thread.

### Data layer — which store owns what

- **DynamoDB** (`LyraLearnTable`, on-demand) — job state, song metadata (including `audioFingerprint`), and vocab/spaced-repetition state. Single-item `Get/UpdateItem` by `PK`/`SK`. `GSI1(PK=userId, SK=createdAt)` — "my songs, newest first". `GSI2(PK=userId, SK=nextReviewAt)` — "vocab due today". `GSI3(PK=audioFingerprint)` — dedup lookup used by the Rust Lambda before running the pipeline.
- **`WebSocketConnections`** (separate DynamoDB table, on-demand) — `connectionId` as PK, `userId` attribute with a GSI for the reverse lookup used by the push handler.
- **MongoDB Atlas (M0 free tier)** — lyrics/translation/word-timing, one document per song, shape mirrors Whisper's nested output. Unique index on `songId`; add a text index on `lines.words.text` only if "find songs containing word X" is actually needed. Watch storage/connection metrics — M0 → M10 is the one line item that can meaningfully move the monthly bill.
- **S3** — `songs/{songId}/{raw|stems|pitch}/...`, SSE-S3, `raw/` transitions to IA after 30 days (stems/pitch stay Standard, read on every playback). SageMaker's execution role is scoped to `songs/*` only, never bucket-wide.

There is no RDS/PostgreSQL in this design (v3 had it; v4 moved vocab/spaced-repetition state to DynamoDB `GSI2` to drop the VPC/NAT Gateway requirement — see `architecture.md` sections 1 and 8 for the reasoning).

Full schemas (DynamoDB item shapes, MongoDB doc shape) and the complete API endpoint table live in `architecture.md` — read it before touching the data layer or adding an endpoint, since field names and index choices there are deliberate.

## Build phases

`architecture.md` section 10 has the authoritative, granular sub-phase breakdown with explicit "done when" conditions for every step — read it before starting work on any phase rather than relying on the summary below. Don't jump ahead (e.g. don't build chunking (2.4-2.6) before the linear pipeline (2.1-2.3) works; don't build the Go WebSocket stack (Phase 6) before Phase 4's polling ships).

1. **Local pipeline, no AWS** — Demucs (two-stems) → faster-whisper (large-v3) → Helsinki-NLP → Basic Pitch on one local file, producing `song_lyrics.json` in the exact MongoDB doc shape.
2. **ML pipeline in AWS** — containerize, wire the linear Step Functions ASL, *then* add chunking (`ChunkAudio`/`Map`/`StitchResults`), then validate timing against the ~70-110s target on real songs.
3. **API layer, auth, dedup** — Cognito + DynamoDB (GSI1/GSI2/GSI3) + `WebSocketConnections` schema, core Python Lambda routes, Rust Lambda validation, *then* fingerprinting/dedup.
4. **Frontend** — scaffold + auth, upload + job status polling, immediate-playback player shell, lyrics hydration, loading/error states.
5. **Learning service** — DynamoDB + Java Lambda skeleton, SM-2 logic (unit-tested in isolation against known reference outputs before wiring to AWS), endpoints, quiz generation, frontend integration.
6. **Real-time and polish** — WebSocket connect/disconnect lifecycle, push-on-completion, frontend WebSocket integration (polling stays as the tested fallback), TensorFlow.js sing-along mode, C++ DSP core only if benchmarking shows Basic Pitch's stock output is insufficient.

## Open decisions (don't treat these as settled)

- **Resolved (Phase 1.3):** translation granularity — line-by-line stands — the Phase 1.3 line-by-line review found only single-line model weaknesses (dropped words, a title-phrase drop, pronoun flips) plus upstream transcription noise, none of which a sliding context window would have fixed (see `notes/phase1.md`).
- Chunk overlap duration (currently ~2-3s) — a tuning knob between stitch quality and redundant compute at the seams; validate against real songs in Phase 2, don't assume the initial estimate is right.
- How aggressively to dedupe via audio fingerprinting — chromaprint matching can false-positive on similar-but-distinct tracks (e.g. two different live recordings); consider a manual review step before auto-linking early on rather than trusting it blindly.
- MongoDB Atlas vs DocumentDB — Atlas first for speed/free tier; DocumentDB only worth revisiting if this ever moves back inside a VPC for other reasons.
- **Resolved (Phase 6.5, 2026-08-01):** C++ DSP core — **explicitly skipped** via the done-when's "confirmed unnecessary" branch: no feature consumes tempo/beat data (pitch.json has no serving endpoint), so a benchmark would measure data with zero readers; Phase 1.4's listener verdict noted no rhythm/timing looseness. Revisit only if a rhythm-consuming feature lands together with the pitch endpoint — see `notes/phase6.md` §6.5.
- Plain Java Lambda vs Spring Cloud Function for the learning service — plain Lambda is the leaner, cheaper default at this scale.
- **Resolved**: Whisper `large-v3` vs `medium` — settled on `large-v3` (via `faster-whisper`) per the Phase 1.2 benchmark, reversing the earlier `medium` default: `medium` missed the song's entire opening chant that `large-v3` caught, and the listener judged `large-v3` better on the real song (see `notes/phase1.md`).

## Security / IAM conventions to follow

- Cognito User Pool is the single identity source; API Gateway's JWT authorizer handles HTTP routes, and the Go `$connect` handler validates the same JWT at connect time — don't introduce a separate auth mechanism.
- IAM roles are scoped per-Lambda-function (e.g. SageMaker role limited to `songs/*`). No wildcard `s3:*`/`dynamodb:*`, no shared execution role across functions or pipeline stages.
- No VPC is used at the current (Tier A, 30-50 user) scale — every component talks over public AWS/internet endpoints with IAM/JWT-based auth instead of network isolation. This is a deliberate tradeoff, not an oversight; revisit only if scale or compliance requirements change.
- Secrets (MongoDB Atlas connection string) go in AWS Secrets Manager, read directly by Lambda at cold start and cached in the execution environment across warm invocations.
