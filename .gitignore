# Python virtual environment
lyralearn-env/
__pycache__/
*.pyc

# Local ML pipeline data — large, regenerable, or copyrighted; never commit
test_data/*
!test_data/.gitkeep
output/

# Rendered infra templates (account-specific values filled in at deploy time)
infra/aws/*.filled.json

# Terraform (lock file .terraform.lock.hcl IS committed)
terraform/.terraform/
*.tfstate
*.tfstate.*

# OS
.DS_Store
Thumbs.db
terraform/modules/api/api_lambda.zip
lambda/validate/target/

# Terraform plan artifacts (never commit - may embed account/resource detail)
tfplan
